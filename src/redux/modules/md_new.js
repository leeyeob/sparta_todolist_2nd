// src/modules/md_new.js

// Action Creator

// Initial State
const initialState = {
	number: 17,
	name: '오투'
};
  
// Reducer
  const MdNew = (state = initialState, action) => {
    switch (action.type) {
      default:
        return state;
    }
  };
  
// export default reducer
  export default MdNew;